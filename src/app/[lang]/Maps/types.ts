export interface MapDictionary {
  map: {
    title: string;
    noItem: string;
    location: string;
    next: string;
    prev: string;
  };
  steps: {
    previous: string;
    next: string;
  };
}
