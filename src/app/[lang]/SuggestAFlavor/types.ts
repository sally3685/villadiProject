export interface suggestDictionary {
  suggestAFlavor: {
    title: string;
    label: string;
    placeHolder: string;
    doneSubmit: string;
  };
  submitStatus: {
    waitSubmit: string;
    submit: string;
  };
  done: {
    success: string;
  };
}
